spring.application.name=TrainingAppDir
# --- DataSource ---
spring.datasource.url=jdbc:mysql://localhost:3306/gym?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Optional but matches your config
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.connection.characterEncoding=utf8

# Security properties
security.jwt.secret-key=HxuBU49AoMrHKNH1GtwKXIznYpHnU9rI7hIN5srnkfa

# 1h in millisecond
security.jwt.expiration-time=3600000